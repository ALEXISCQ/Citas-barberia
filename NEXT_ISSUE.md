# Siguiente Issue Disponible

## Issues desbloqueados
Basado en el progreso actual del repo y los cambios consolidados:

- `#1 Configurar backend FastAPI con MongoDB` — completado.
- `#2 Crear modelo de cita en MongoDB` — completado.
- `#3 Crear API para citas` — completado.
- `#4 Permitir asignar citas desde el frontend` — completado.
- `#5 Mostrar citas programadas en el frontend` — completado.

## Issue liberado
- `#6 Gestionar estados de cita` está ahora desbloqueado y es el siguiente ticket lógico para el siguiente ciclo.

## Dependencias previstas
- `#7 Mostrar disponibilidad de citas` dependerá de `#6`.
- `#9 Agregar validación y manejo de errores` puede avanzar tras `#4` y `#5`.

## Ralph
- El script `ralph/once.sh` no existe en este repositorio actualmente.
- Por tanto, no se puede ejecutar el ciclo autónomo `./ralph/once.sh <ID_DEL_SIGUIENTE_ISSUE>` aquí hasta que se agregue el script.
